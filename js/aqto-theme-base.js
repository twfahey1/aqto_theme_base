/**
 * @file
 * aqto_theme_base behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.aqtoThemeBase = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
